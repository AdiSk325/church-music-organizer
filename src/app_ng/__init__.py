"""NiceGUI desktop-first UI for Church Music Organizer.

Replaces the legacy single-file Streamlit app (archived in ``src/app/_legacy/``).
Entry point: ``src/app_ng/main.py`` (``poetry run python -m src.app_ng.main``).
"""
